Сегодня я разобрался с работой магического метода def __new__(self)

---
def __new__(self, *args, **kwargs) - вызывается перед созданием объекта

def __init(self, *params) - вызывается после создания объекта